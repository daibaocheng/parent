<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vankeytech.pmp.hms.mapper.UserMapper">
  <resultMap id="BaseResultMap" type="com.vankeytech.pmp.hms.entity.User">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="user_id" jdbcType="INTEGER" property="userId" />
    <result column="user_name" jdbcType="VARCHAR" property="userName" />
    <result column="nick_name" jdbcType="VARCHAR" property="nickName" />
    <result column="account_no" jdbcType="BIGINT" property="accountNo" />
    <result column="sex" jdbcType="BIGINT" property="sex" />
    <result column="nationality" jdbcType="VARCHAR" property="nationality" />
    <result column="native_place" jdbcType="VARCHAR" property="nativePlace" />
    <result column="permanent_address" jdbcType="VARCHAR" property="permanentAddress" />
    <result column="identity_card" jdbcType="VARCHAR" property="identityCard" />
    <result column="phone1" jdbcType="VARCHAR" property="phone1" />
    <result column="phone2" jdbcType="VARCHAR" property="phone2" />
    <result column="email" jdbcType="VARCHAR" property="email" />
    <result column="building_id" jdbcType="BIGINT" property="buildingId" />
    <result column="house_proprietary_certificate" jdbcType="VARCHAR" property="houseProprietaryCertificate" />
    <result column="company_name" jdbcType="VARCHAR" property="companyName" />
    <result column="post" jdbcType="VARCHAR" property="post" />
    <result column="company_address" jdbcType="VARCHAR" property="companyAddress" />
    <result column="identity_card_photo_obverse" jdbcType="VARCHAR" property="identityCardPhotoObverse" />
    <result column="identity_card_photo_reverse" jdbcType="VARCHAR" property="identityCardPhotoReverse" />
    <result column="face_photo" jdbcType="VARCHAR" property="facePhoto" />
    <result column="landlord" jdbcType="BIGINT" property="landlord" />
    <result column="display" jdbcType="BIGINT" property="display" />
    <result column="remarks" jdbcType="VARCHAR" property="remarks" />
  </resultMap>

  <sql id="baseSql">
    user.user_id,user.user_name,user.nick_name,user.account_no,user.sex,user.nationality,user.native_place,user.permanent_address,
    user.identity_card,user.phone1,user.phone2,user.email,user.building_id,user.house_proprietary_certificate,user.company_name,user.post,
    user.company_address,user.identity_card_photo_obverse,user.identity_card_photo_reverse,user.face_photo,user.landlord,user.display,user.remarks
  </sql>

  <!-- 批量插入数据 -->
  <!--<insert id="insertUsers" parameterType="list">
    INSERT INTO hms_user VALUES
    <foreach collection="list" item="user" separator=",">
      (#{user.userId},#{user.userName},#{user.nickName},#{user.accountNo},#{user.sex},#{user.nationality},#{user.nativePlace},#{user.permanentAddress},
      #{user.identityCard},#{user.phone1},#{user.phone2},#{user.email},#{user.buildingId},#{user.houseProprietaryCertificate},#{user.companyName},#{user.post},
      #{user.companyAddress},#{user.identityCardPhotoObverse},#{user.identityCardPhotoReverse},#{user.facePhoto},#{user.landlord},#{user.display},#{user.remarks})
    </foreach>
  </insert>-->

  <!-- 批量删除数据 -->
  <!--<delete id="deleteUsers" parameterType="list">
    DELETE FROM hms_user
    WHERE user_id IN
    <foreach collection="list" open="(" close=")" separator=",">
      #{id}
    </foreach>
  </delete>-->

  <!-- 批量删除数据 -->
  <update id="deleteByPrimaryKey" parameterType="list">
    UPDATE FROM hms_user u SET USER.display=0
    WHERE u.user_id=#{id}
  </update>
</mapper>